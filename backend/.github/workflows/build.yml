name: CI Job

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: "Build"
        run: |
          set -e
          set -x
          python3 -m pip install mypy==1.3.0
          make svc-api-dep svc-api-dev
          make mypy-analyse
          make tnt-onboarding-build
