import DefaultLayout from '../../business-logic/layout/DefaultLayout';
import Taxonomy from '../../business-logic/taxonomy/index';

export default function TaxonomyPage() {
    return (
        <DefaultLayout>
            <Taxonomy />
        </DefaultLayout>
    );
}
