{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn --cwd apps/home dev",
    "build": "yarn --cwd apps/home build",
    "start": "yarn --cwd apps/home start",
    "update": "yarn upgrade-interactive --latest"
  },
  "dependencies": {
    "@types/jest": "^29.5.10",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1"
  }
}
